
# Johnnny and claude code together

Claude and I had such an interesting time i recorded some of it.

[[]]



Claude,
you were right - but for the wrong reasons.

Look at the following screenshot i took from the GUI app. 

It shows the current state of the wiring which ought be be reflected in the 
BenchConfig you pushed to it.

Do you see the inputs and outputs circle at the bottom with nothing connected? that is the problem. 😅


![[volo_vhdl/docs/claude-and-johnny-debug-together/Pasted image 20251024194510.png]]


Each bench also has a CyberPower 8-port network controllable PDU. I dont want to get bogged down in the details but that should be assigned per bench.

Each bench generally will also have a Target (or DUT). 

For now 'DUT' will just be a placeholder model with a name / description. nothing detailed

  2. Physical Wiring - How should we model probe→Moku connections?
    - I think the definition of 'How a bench is wired' is precisely the same as, 'What devices attached to what Physical Inputs of the benches Moku device'
We should encapsulate/re-use a moku physical platform for this. right? 

3. i **dont** think we need to clutter it up with the 'ExternalHardware' - if it is not wired to a physical moku platforms input or output it is not our problem.
if it is wired to one it will be in the moku platform model. 


models/bench/benchbench.py for sure. I love the absurd triple bench
