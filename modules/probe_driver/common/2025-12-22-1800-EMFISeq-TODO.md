

## CLAUDE-MCC-SET-REGS-CoCoTb


Claude,
create a a new branch: feature/MCC-SetRegsTB and switch to it

Connect to Serena over MCP and look for stored memories related to cocotb and testbenches.

I want you to help me introduce a new cocotb 'primitive' ( i really dont know the word). 

You may not have known it, but the moku family of devices actually provides an api call ('`set_regs`) that lets us update the values of the top level mcc control registers over the network.

All higher level applications we develop depend on  this feature to remotely control functionality of our custom bitstreams.

I would like you to 
1) help me add a new mcc_set_regs function into the cocotb unit tests
2) update serena memories about mcc_set_regs. 
3) commit it merge it etc

It is important to keep in mind that:
- the `mcc_set_regs` interface is __completely asyncronous__ (this should make it a good fit for cocotb.)

- we should actually introduce random jitter (approximately 250ms worth) for all calls to mcc_set_regs.  

Do you think this is a good fit? Are there other things we should consider? 

# 2025-12-22-1800-EMFISeq

Great news. Our MCC bitstream compiled and deployed correctly on the first try. Now for the next iteration.

## R1: I would like the entire state machine module to stop once it reaches S4. (We can think of this stage as 'DONE')

## R2:  would like to be able to easily re-set the state machine all the way from the MCC-TOP Control registers. 

The idea is that the state machine will cycle once, and the user will set a specific control-bit high (over the network) and allow it to continue again. 

## R3 
On reset the state machine should refresh its input delays (i dont think this is actually a change)

## R4
On reset the analog monitor out should refresh its staircase levels (from the top level MCC control regs)


Ask follow up questions ,and then come up with a plan to implment the changes, as well as testbenches that validate them.


This will be the first time we 'reset' any sub-module independently of the entire system, so it may expose other issues.
]



Great! I've successfully created the branch feature/MCC-SetRegsTB and read the
  cocotb-related memories.

  Looking at the current conftest.py, I can see you have several categories of test utilities:
  - Clock Management
  - Reset Sequences
  - Signal Monitoring
  - Initialization Helpers
  - Division Ratio Testing
  - Assertion Helpers
  - Waveform Helpers
  - Module-Specific Helpers (placeholder)

  Based on your branch name "MCC-SetRegsTB", I suspect you want to add a helper for MCC
  control register initialization. I noticed in the cocotb_testing_guide memory there's an
  example pattern for init_control_registers that's currently just shown as example code.

  What specific cocotb helper/primitive would you like me to add? Some possibilities:

  1. MCC register initialization helper - A reusable init_control_registers() function
  2. MCC register setter utilities - Functions to pack/set specific register fields
  3. MCC input/output initialization - Helpers for InputA-D and OutputA-D
  4. Something else entirely?

  Please describe what functionality you need, and I'll help you implement it as a proper
  shared utility in conftest.py!

--
Actually you kind of nailed it. I think I have a few interrelated initialization concerns that you can help address in one go.

## T1: The Moku-MCC hardware initialization process
Althought there are moku API's for 
- loading a bitstream
- setting the control registers

The actual hardware initialization appears (to me), to go something like this:

### S1) bitstream is uploadaed, MCC-Top level reset is applied
- I **believe** the clock actually gets started with all MCC-Top level control registers initialized to zero.. then, 
#### S2) Time passess
Like, potentially seconds

#### S3) MCC-Top / CustomWrapper CR's updated
From the outside, over the network
#### S4)  The MCC application continues


Although I suspect we will gain the ability to specify initial starting registers in the future, in the meantime I propose the following potential solution.


## A potential solution (testbenches and hardware)
Given these somewhat..harsh startup conditions IRL, I propose something like the following

(See @EMFI_Seq/top for an example)

## In the cocotb test simulator
- we should probably mimic this kind of harsh initialization process

## In our own MCC-Top
I think we should take advantage of this early 'all-zeros' initialization phase to treat is as some sort of 'bring-up'/'setting' period, and __somehow__ 

### Map the inverse of CR0[31] to a global_enable signal? 
I'm open to suggestions on what to call it, and what specifically to do before we observe it change, but in general
- 'user' level VHDL (i.e. everything we generate inside this environment) should be automatically insulated to this. Only the highest-level testbench objects should really need to know. 


Think about potential solutions to what I vote we call the 'all-zero-reset-state' and let me know what you think. 


After we figure that out, we should talk about implementing an mcc_set_regs test primitive.
Fun fact: I think we should imitate network latency on that one.




